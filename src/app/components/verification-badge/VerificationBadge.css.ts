import { style } from "@vanilla-extract/css";

export const VerificationBadge = style({
    marginLeft: '4px',
    padding: '0 4px 0 2px',
    borderRadius: '20px',
});

export const VerificationBadgeDiv = style({
    display: 'flex',
    gap: '4px'
});